<div class="section index">
  <div class="container">
    <h1 class="index-logo">GOODALL</h1>
    <form class="form" method="post" {{action "searchLocation" search on="submit"}}>
      <div class="form__block">
        {{input type="text" placeholder="Location" class="form__input" value=search}}
        {{search-filter}}
        <span><button type="submit" id=1 name="Go" class="btn btn-form btn-fw">Search</button></span>
      </div>
    </form>
    <div class="index-grid">
      {{#each model as |event|}}
      {{!-- {{event-index-card}} --}}
        <span class="index-grid__item">{{event.title}}</span>
      {{/each}}
    </div>
  </div>
  {{outlet}}
</div>
